import React from 'react'
import './gradient.css'

const Gradient = (props) => {
    return (
        <div
            style={{ background: props.background }}
            className="gradient"
        >
        </div>
    )
}

export default Gradient
